<!-- BUG (IT WORKS) HEADER TEMPLATE check this out (currentPlant problem)-->

<!-- NORMAL BEHAVIOUR : IFs don't work cause currentPlant has been changed for selectedPlant in controllers-->

<!-- BUT it's probably the only place where it hasn't been changed -->
<!-- could it be the root of all evil ???????????? check also code around that tpl -->

{% if currentPlant is defined %}
<section class="ftco-section ftco-bread" style="background-image: url({{ asset('uploads/avatars/' ~ currentPlant.cover) }})">
{% else %} 
<section class="ftco-section ftco-bread">
{% endif %}

<div class="container">
    <div class="row no-gutters slider-text justify-content-center align-items-center">
        <div class="col-md-8 ftco-animate">


            {% if currentPlant is defined %}
                <h1 class="bread">{{ currentPlant.name }}</h1>
            {% else %} 
                <h1 class="bread">Lettuce seed</h1>
            {% endif %}            

            <!-- NOTICE BREADCRUMBS check auth if "really" adding breadcrumbs here -->
            <!-- for now, keeping class for style purposes only -->
            <p class="breadcrumbs">
                Tracking plants with ease. 
            </p>

        </div>
    </div>
</div>
</section>
