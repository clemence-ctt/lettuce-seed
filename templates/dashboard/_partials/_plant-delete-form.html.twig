<form class="delete" method="post" action="{{ path('dashboard_plant_delete', {'id': plant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plant.id) }}">
    <button class="button">Delete this plant</button>
</form>
