<div>
    {{ form_start(form, {
        'attr': {
            'class': 'sf-form'
        }
    }) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.date) }}
    {{ form_row(form.file) }} 
        {# Décommenter si edit form = new form #}
        {# {% if form.vars.data is not null and form.vars.data.file is not null %} #}
        <img src="{{ asset('uploads/pictures/' ~ oldPictureFile) }}"/>
        {# {% endif %} #}
    {{ form_row(form.plants) }}

    <button class="button">{{ button_label|default('Update') }}</button>
   
{{ form_end(form) }}

<div class="required-info"><i class="fas fa-star-of-life"></i> Required fields</div>
</div>